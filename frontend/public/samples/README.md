Put three test-split X-rays here as `sample-1.png`, `sample-2.png`, `sample-3.png`.

Notebook cell 24 writes them to `artifacts/samples/` along with `samples.json`
(the expected finding and ground-truth labels for each). Copy them across.

They must come from the TEST split, never train — showing off on training images
is the demo equivalent of reporting training accuracy.
